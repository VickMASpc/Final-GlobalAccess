window.App = window.App || {};

window.App.getFirebaseStatus = function getFirebaseStatus() {
  return {
    connected: false,
    message: 'Firebase adapter placeholder. Remote sync will be implemented in a later milestone.',
  };
};
